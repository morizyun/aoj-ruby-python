#include <iostream>
#include <math.h>

using namespace std;

float length(float x1, float y1, float x2, float y2) {
  return pow(pow(x1-x2, 2.0) + pow(y1-y2, 2.0), 0.5);
}

int main() {
  string str;
  for(int i = 0; i < 8; i++) {
    cin >> n[i];
  }
  while (cin >> str) {
     cout << str << endl;
  }
}